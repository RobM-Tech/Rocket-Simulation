[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "rocket-sim"
version = "1.1.0"
description = "Physics-based rocket launch and telemetry simulation"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["src/rocket_sim"]